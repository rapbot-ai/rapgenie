# Pinned, same reasoning as ../training/requirements.txt: torch, torchaudio,
# numpy, scipy, unidecode etc. are owned by the vendored radtts fork's own
# requirements.txt (installed first in the Dockerfile), not redeclared here.
#
# Deliberately smaller than training's requirements.txt: no pyyaml (inference
# config comes straight from the JSON job payload, never a YAML file — see
# ./config.py), no wandb (no run tracking for a single inference
# request), no google-cloud-storage (this worker only ever talks to S3 via
# blob_storage.blob_storage.build_blob_store).
boto3==1.34.131

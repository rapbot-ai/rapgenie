# Deps for download_run_history.py only. This script never runs inside the
# Docker image — src/training/requirements.txt governs that, and pins its
# packages against the vendored RADTTS fork's own numpy/pandas/etc, which
# this file has nothing to do with. Kept separate on purpose: mixing a
# local-only dev tool's deps into the container's manifest is exactly the
# kind of version-pinning conflict that file's own header comment warns
# about, for a script that never even runs in that environment.
#
# Install into your own venv:
#   python3 -m venv .venv && source .venv/bin/activate && \
#     pip install -r src/training/scripts/requirements.txt
wandb==0.28.0
pandas==2.2.2
pyyaml==6.0.1
